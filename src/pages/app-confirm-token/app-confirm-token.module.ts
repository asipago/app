import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AppConfirmTokenPage } from './app-confirm-token';

@NgModule({
  declarations: [
    AppConfirmTokenPage,
  ],
  imports: [
    IonicPageModule.forChild(AppConfirmTokenPage),
  ],
})
export class AppConfirmTokenPageModule {}
